<%= form_for(@feedback_form) do |f| %>
  <% if @feedback_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback_form.errors.count, "error") %> prohibited this feedback_form from being saved:</h2>

      <ul>
      <% @feedback_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :register_number %><br>
    <%= f.text_field :register_number %>
  </div>
  <div class="field">
    <%= f.label :workshop %>
    <%= f.collection_select :workshop_id, Workshop.all, :id, :title, prompt: "Select Workshop" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
